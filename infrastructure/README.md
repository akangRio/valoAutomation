# Infrastructure Directory

This directory contains system configurations, environment variables examples, build scripts, local deployment automation templates, and Windows task scheduler templates.

## Contents

- **`env/`**: `.env` configuration files for dev/prod.
- **`windows/`**: `.bat` files or XML schedules to configure background execution on startup.
